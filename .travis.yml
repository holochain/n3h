dist: trusty

language: node_js
node_js:
  - 8

jobs:
  include:
    - name: "windows"
      os: windows
      script:
        - npm test
    - name: "linux"
      os: linux
      script:
        - npm test
    - name: "osx"
      os: osx
      script:
        - npm test

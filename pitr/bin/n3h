#!/usr/bin/env node
const { Pitr } = require('../lib/index')

async function main () {
  const pitr = new Pitr()
  await pitr.run()
  await pitr.destroy()
}

main().then(() => {
  console.log('up and running')
}, (err) => {
  console.error(err)
  process.exit(1)
})
